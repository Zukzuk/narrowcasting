services:
  komga-app:
    image: zukzuk/narrowcasting:${VERSION_TAG}
    ports:
      - "3001:3000"
    env_file:
      - public.env
      - private.env
    restart: always
    networks:
      - shared
    environment:
      - VERSION_TAG=${VERSION_TAG}

networks:
  shared:
    external: true